============
Installation
============

At the command line::

    $ easy_install flask-musers

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flask-musers
    $ pip install flask-musers